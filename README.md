# transvolt-backend
NodeJS --- Express ---- PostgresSQL

Create a `.env` file from root
and add variables `JWT_SECRET`, `ACCESS_SECRET`, `REFRESH_SECRET` with HS256 generated secret. 

Run `npm install` from root
After that `node server.js` from root
